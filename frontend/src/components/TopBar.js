import React from 'react';

const TopBar = () => {
    return (
        <div className="top-bar">
            <div className="logo">Multi:verse</div>
            <button className="login-btn">login</button>
        </div>
    );
};

export default TopBar;